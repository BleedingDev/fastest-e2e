{
  "name": "Save a display name",
  "url": "https://your-deployed-app.example/settings",
  "goal": "Change the display name to E2E Example, save it, and stop when the page confirms it was saved.",
  "checks": [
    { "kind": "value", "selector": "input[name=displayName]", "value": "E2E Example" }
  ],
  "timeoutMs": 120000,
  "maxSteps": 30,
  "checkTimeoutMs": 5000,
  "keepTab": true,
  "changeRef": "Replace with the PR or feature reference; this is metadata, not deployment proof."
}
